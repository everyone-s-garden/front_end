import React, { useState } from 'react';
import styled from 'styled-components';
const AfterLogin = () => {
  return <Container>is After</Container>;
};

export default AfterLogin;

const Container = styled.section`
  width: fit-content;
  height: fit-content;
`;

// const Box = styled.div`
//   width: 600px;
//   height: 600px;
//   cursor: pointer;
// `;

{
  /* <Box onClick={() => setAnimate(prev => !prev)}>
<Lottie animationData={Data} loop={animage ? true : false}></Lottie>
</Box> */
}
